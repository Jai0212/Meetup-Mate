<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MeetupMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MeetupMate" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="MeetupMateProfile" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="Theme.MeetupMate.NoActionBar.Back" parent="Theme.MaterialComponents.DayNight"/>
</resources>